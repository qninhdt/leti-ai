// Generated by @qninhdt/typespec-protobuf. DO NOT EDIT.

// emitter: @qninhdt/typespec-protobuf@0

syntax = "proto3";

package openlet.file.v1;

import "google/protobuf/timestamp.proto";

message GetFileRequest {
  reserved 100 to 199;
  string file_id = 1;
  // include_text — when true, GetFile populates FileInfo.extracted_text.
  // Wire-compatible additive flag; existing callers default to false.
  bool include_text = 2;
}

message FileInfo {
  reserved 100 to 199;
  string file_id = 1;
  string workspace_id = 2;
  string folder_id = 3;
  string name = 4;
  int64 size_bytes = 5;
  string mime_type = 6;
  string magika_label = 7;
  string status = 8;
  google.protobuf.Timestamp created_at = 9;
  google.protobuf.Timestamp updated_at = 10;
  // tags — denormalized list returned by GetFile / ListFiles. Mirrors
  // the per-file tag set served by the HTTP /files/{id}/tags route so
  // gRPC callers do not need a separate fetch.
  repeated string tags = 11;
  // extracted_text — populated only when GetFileRequest.include_text =
  // true. Empty otherwise to keep the default response payload bounded.
  string extracted_text = 12;
}

message ListWorkspaceFilesRequest {
  reserved 100 to 199;
  string workspace_id = 1;
  string folder_id = 2;
  int32 page_size = 3;
  string page_token = 4;
}

message ListWorkspaceFilesResponse {
  reserved 100 to 199;
  repeated FileInfo files = 1;
  string next_page_token = 2;
}

message Workspace {
  reserved 100 to 199;
  string workspace_id = 1;
  string name = 2;
  string description = 3;
  string owner_principal_type = 4;
  string owner_principal_id = 5;
  google.protobuf.Timestamp created_at = 6;
  google.protobuf.Timestamp updated_at = 7;
}

message Member {
  reserved 100 to 199;
  string workspace_id = 1;
  string principal_type = 2;
  string principal_id = 3;
  string role = 4;
  string invited_by_user_id = 5;
  google.protobuf.Timestamp invited_at = 6;
  google.protobuf.Timestamp accepted_at = 7;
  string display_name = 8;
  string avatar_url = 9;
}

message ListWorkspacesRequest {
  reserved 100 to 199;
  int32 page_size = 1;
  string page_token = 2;
}

message ListWorkspacesResponse {
  reserved 100 to 199;
  repeated Workspace workspaces = 1;
  string next_page_token = 2;
}

message GetWorkspaceRequest {
  reserved 100 to 199;
  string workspace_id = 1;
}

message CreateWorkspaceRequest {
  reserved 100 to 199;
  string name = 1;
  string description = 2;
}

message PatchWorkspaceRequest {
  reserved 100 to 199;
  string workspace_id = 1;
  string name = 2;
  string description = 3;
}

message DeleteWorkspaceRequest {
  reserved 100 to 199;
  string workspace_id = 1;
}

message DeleteWorkspaceResponse {
  reserved 100 to 199;
}

message ListMembersRequest {
  reserved 100 to 199;
  string workspace_id = 1;
}

message ListMembersResponse {
  reserved 100 to 199;
  repeated Member members = 1;
}

message AddMemberRequest {
  reserved 100 to 199;
  string workspace_id = 1;
  string principal_type = 2;
  string principal_id = 3;
  string role = 4;
}

message PatchMemberRequest {
  reserved 100 to 199;
  string workspace_id = 1;
  string principal_type = 2;
  string principal_id = 3;
  string role = 4;
}

message RemoveMemberRequest {
  reserved 100 to 199;
  string workspace_id = 1;
  string principal_type = 2;
  string principal_id = 3;
}

message RemoveMemberResponse {
  reserved 100 to 199;
}

message Folder {
  reserved 100 to 199;
  string folder_id = 1;
  string workspace_id = 2;
  string parent_folder_id = 3;
  string name = 4;
  string created_by_user_id = 5;
  google.protobuf.Timestamp created_at = 6;
  google.protobuf.Timestamp updated_at = 7;
}

message ListFoldersRequest {
  reserved 100 to 199;
  string workspace_id = 1;
  string parent_folder_id = 2;
  int32 page_size = 3;
  string page_token = 4;
}

message ListFoldersResponse {
  reserved 100 to 199;
  repeated Folder folders = 1;
  string next_page_token = 2;
}

message GetFolderRequest {
  reserved 100 to 199;
  string folder_id = 1;
}

message CreateFolderRequest {
  reserved 100 to 199;
  string workspace_id = 1;
  string parent_folder_id = 2;
  string name = 3;
}

message PatchFolderRequest {
  reserved 100 to 199;
  string folder_id = 1;
  string name = 2;
  string parent_folder_id = 3;
}

message DeleteFolderRequest {
  reserved 100 to 199;
  string folder_id = 1;
}

message DeleteFolderResponse {
  reserved 100 to 199;
}

message ListFilesRequest {
  reserved 100 to 199;
  string workspace_id = 1;
  string folder_id = 2;
  int32 page_size = 3;
  string page_token = 4;
}

message ListFilesResponse {
  reserved 100 to 199;
  repeated FileInfo files = 1;
  string next_page_token = 2;
}

message CreateUploadIntentRequest {
  reserved 100 to 199;
  string workspace_id = 1;
  string folder_id = 2;
  string name = 3;
  int64 size_bytes = 4;
  string mime_type = 5;
}

message CreateUploadIntentResponse {
  reserved 100 to 199;
  string file_id = 1;
  string upload_url = 2;
  google.protobuf.Timestamp upload_url_expires_at = 3;
  string storage_key = 4;
}

message CompleteUploadRequest {
  reserved 100 to 199;
  string file_id = 1;
  string etag = 2;
  int64 size_bytes = 3;
}

message PatchFileRequest {
  reserved 100 to 199;
  string file_id = 1;
  string name = 2;
}

message DeleteFileRequest {
  reserved 100 to 199;
  string file_id = 1;
}

message DeleteFileResponse {
  reserved 100 to 199;
}

message MoveFileRequest {
  reserved 100 to 199;
  string file_id = 1;
  string folder_id = 2;
}

message SearchHit {
  reserved 100 to 199;
  string file_id = 1;
  string workspace_id = 2;
  string folder_id = 3;
  string name = 4;
  string snippet = 5;
  double score = 6;
}

message SearchRequest {
  reserved 100 to 199;
  string workspace_id = 1;
  string query = 2;
  repeated string tags = 3;
  int32 page_size = 4;
  string page_token = 5;
}

message SearchResponse {
  reserved 100 to 199;
  repeated SearchHit hits = 1;
  string next_page_token = 2;
}

// SearchFilesRequest — Leti's full-text + metadata file search. Returns
// FileInfo (not SearchHit) so callers do not need a follow-up GetFile.
message SearchFilesRequest {
  reserved 100 to 199;
  string workspace_id = 1;
  string query = 2;
  repeated string mime_prefixes = 3;
  repeated string tags = 4;
  google.protobuf.Timestamp updated_after = 5;
  int32 limit = 6;
  string page_token = 7;
}

message SearchFilesResponse {
  reserved 100 to 199;
  repeated FileInfo files = 1;
  string next_page_token = 2;
}

// GrepFilesRequest — agent `grep -r` prefilter for openlet-ai's cloud
// Filesystem. `literals` are substrings (>= 3 chars) extracted from the
// caller's regex; the backend runs a trigram-accelerated ILIKE-ANY prefilter
// over them. The caller-controlled regex is NEVER sent — the exact re-match
// runs in-process on openlet-ai (linear-time Rust `regex`). Empty `literals`
// means "no extractable literal": the backend returns the bounded ready set
// and the in-process re-match does all the filtering.
message GrepFilesRequest {
  reserved 100 to 199;
  string workspace_id = 1;
  repeated string literals = 2;
  int32 max_hits = 3;
}

// GrepCandidate — one prefilter hit. `extracted_text` is the (possibly
// truncated) indexed body the caller re-matches against; `folder_id` is empty
// for workspace-root files.
message GrepCandidate {
  reserved 100 to 199;
  string file_id = 1;
  string name = 2;
  string folder_id = 3;
  string extracted_text = 4;
}

message GrepFilesResponse {
  reserved 100 to 199;
  repeated GrepCandidate candidates = 1;
}

message Tag {
  reserved 100 to 199;
  string workspace_id = 1;
  string name = 2;
  int64 file_count = 3;
}

message ListTagsRequest {
  reserved 100 to 199;
  string workspace_id = 1;
}

message ListTagsResponse {
  reserved 100 to 199;
  repeated Tag tags = 1;
}

message AddFileTagsRequest {
  reserved 100 to 199;
  string file_id = 1;
  repeated string tags = 2;
}

message AddFileTagsResponse {
  reserved 100 to 199;
  repeated string tags = 1;
}

message RemoveFileTagRequest {
  reserved 100 to 199;
  string file_id = 1;
  string tag = 2;
}

message RemoveFileTagResponse {
  reserved 100 to 199;
}

service FileService {
  rpc ListWorkspaces(ListWorkspacesRequest) returns (ListWorkspacesResponse);
  rpc GetWorkspace(GetWorkspaceRequest) returns (Workspace);
  rpc CreateWorkspace(CreateWorkspaceRequest) returns (Workspace);
  rpc PatchWorkspace(PatchWorkspaceRequest) returns (Workspace);
  rpc DeleteWorkspace(DeleteWorkspaceRequest) returns (DeleteWorkspaceResponse);
  rpc ListMembers(ListMembersRequest) returns (ListMembersResponse);
  rpc AddMember(AddMemberRequest) returns (Member);
  rpc PatchMember(PatchMemberRequest) returns (Member);
  rpc RemoveMember(RemoveMemberRequest) returns (RemoveMemberResponse);
  rpc ListFolders(ListFoldersRequest) returns (ListFoldersResponse);
  rpc GetFolder(GetFolderRequest) returns (Folder);
  rpc CreateFolder(CreateFolderRequest) returns (Folder);
  rpc PatchFolder(PatchFolderRequest) returns (Folder);
  rpc DeleteFolder(DeleteFolderRequest) returns (DeleteFolderResponse);
  rpc ListFiles(ListFilesRequest) returns (ListFilesResponse);
  rpc GetFile(GetFileRequest) returns (FileInfo);
  rpc ListWorkspaceFiles(ListWorkspaceFilesRequest) returns (ListWorkspaceFilesResponse);
  rpc CreateUploadIntent(CreateUploadIntentRequest) returns (CreateUploadIntentResponse);
  rpc CompleteUpload(CompleteUploadRequest) returns (FileInfo);
  rpc PatchFile(PatchFileRequest) returns (FileInfo);
  rpc DeleteFile(DeleteFileRequest) returns (DeleteFileResponse);
  rpc MoveFile(MoveFileRequest) returns (FileInfo);
  rpc Search(SearchRequest) returns (SearchResponse);
  rpc SearchFiles(SearchFilesRequest) returns (SearchFilesResponse);
  rpc GrepFiles(GrepFilesRequest) returns (GrepFilesResponse);
  rpc ListTags(ListTagsRequest) returns (ListTagsResponse);
  rpc AddFileTags(AddFileTagsRequest) returns (AddFileTagsResponse);
  rpc RemoveFileTag(RemoveFileTagRequest) returns (RemoveFileTagResponse);
}
