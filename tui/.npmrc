; opentui-spinner@0.0.6 declares a stale @opentui/core peer (^0.1.49) against
; the pinned ^0.2.x set, so strict peer resolution fails without this. Recorded
; here (not just in CI) so local `npm ci`/`npm install` matches the CI lane.
legacy-peer-deps=true
